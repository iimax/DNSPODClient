<Window x:Class="DNSPODClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DNSPODClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!--主界面：域名列表-->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="10"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Width="84" Background="#2b8a00" Margin="10,0">添加域名</Button>
                <Button Width="84" Margin="10,0">暂停</Button>
                <Button Width="84" Margin="10,0">启用</Button>
                <Button Width="84" Margin="10,0">删除</Button>
            </StackPanel>

            <ListBox Grid.Row="2">
                <ListBoxItem>
                    dnspod.cn
                </ListBoxItem>
            </ListBox>
        </Grid>
    </Grid>
</Window>
